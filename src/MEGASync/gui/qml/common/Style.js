function getCategoryValue(categoryId, tokenId) {
    return (themeManager.theme, categoryStyle.getValue(categoryId, tokenId));
}

function getColorValue(tokenId){
    return (themeManager.theme, colorStyle.getValue(tokenId));
}
